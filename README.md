# workflow-bootstrap-test

Repo **descartavel** para testar o bootstrap do pipeline padrao (repo sem
nenhuma tag, primeira release, casos de borda que nao dao para reproduzir na
bancada principal).

A referencia do padrao e `filipesfbr/workflow-test`.
